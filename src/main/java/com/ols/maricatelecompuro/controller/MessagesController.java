package com.ols.maricatelecompuro.controller;


import com.ols.maricatelecompuro.controller.dto.AlunoDto;
import com.ols.maricatelecompuro.model.Aluno;
import com.ols.maricatelecompuro.model.Message;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Arrays;
import java.util.List;

@RestController
public class MessagesController {
/*  @RequestMapping("/InstMessageGeral")
  public List<Message> sendGeneralMessage(){

    return Arrays.asList(message);
  }
  @RequestMapping("/InstMessagePersonalizada")
  public List<AlunoDto> sendMessage(){
    return AlunoDto.converter(Arrays.asList(aluno));
  }*/
}
